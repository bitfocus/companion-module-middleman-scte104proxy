module.exports = {
	WS: undefined, //websocket connection to service

	DATA: {
		//data object to store variables
		inputs: [{ asIndex: -1 }], //array of input objects
	},

	POLLING_INTERVAL: undefined, //interval for polling
}
